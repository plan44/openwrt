blkdevparts=mmcblk0:32K(env),512K@32K(idblock),512K(uboot),32M(boot),-(rootfs)
sys_bootargs= root=/dev/mmcblk0p5 rootfstype=squashfs,f2fs noinitrd rw rootwait rk_dma_heap_cma=66M
# Override compiled-in bootcmd (evb_rv1106.h defines "reset" when
# CONFIG_SPL_KERNEL_BOOT=y, on the assumption SPL loads the kernel directly).
# Our SPL falls through to U-Boot proper instead, so without this override the
# board reset-loops. boot_fit loads the FIT kernel from the "boot" partition.
bootcmd=boot_fit
